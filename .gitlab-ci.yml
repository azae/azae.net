build_image:
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker login registry.gitlab.com -u tclavier -p $DOCKER_TOKEN
    - docker build -t registry.gitlab.com/azae/site .
    - docker push registry.gitlab.com/azae/site
